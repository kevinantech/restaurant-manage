const ADMIN_ROOT = "/admin";

export enum EAdminPaths {
  DASHBOARD = ADMIN_ROOT,
  PRODUCTS = ADMIN_ROOT + "/products",
  SETTINGS = ADMIN_ROOT + "/settings",
  CATEGORY = ADMIN_ROOT + "/category",
}

export enum WebRoutes {
  LOGIN = '/login',
  REGISTER = '/register',
  HOME = '/home',
  INVENTORY = '/inventory',
}

export enum ApiRoutes {
  INVENTORY = '/api/inventory',
  PRODUCTS = '/api/products',
  ORDERS = '/api/orders',
}

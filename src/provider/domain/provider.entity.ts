export interface Provider {
  id: string;
  name: string;
  email: string;
  phone: string;
  address: string;
  contact: string;
}

export interface InsertProvider {
  name: string;
  email: string;
  phone: string;
  address: string;
  contact: string;
}
